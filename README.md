ClusterFuzzLite corpus + coverage storage for robustfengbin/zebra-fuzz-m3.

- `main`: minimized per-target seed/accumulated corpus (corpus/<target>/)
- `gh-pages`: coverage report published by the daily cron.
